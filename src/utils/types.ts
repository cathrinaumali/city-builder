export type House = {
    id: string;
    name: string;
    floors: number;
    color: string;
};

export type HouseListProps = {
    id: number;
    name: string;
    floors: number;
    color: string;
};