'use client';
import { Stage, Layer, Rect, Line, Text } from 'react-konva';


type CityBuilderFormProps = {
  
};

const CityBuilderForm = ({  }: CityBuilderFormProps) => {
  
  return (
   <></>
  );
};

export default CityBuilderForm;

